---
import Layout from '../layouts/Layout.astro';
import Icon from '../components/Icons.astro';
import Breadcrumb from '../components/Breadcrumb.astro';
import { getPlatformStats } from '../lib/meilisearch';

const pais = Astro.locals.country?.name || '';

let stats = { totalProperties: 0, totalAgents: 0, totalCities: 0 };
try {
  stats = await getPlatformStats(pais);
} catch (error) {
  console.error('Error fetching platform stats:', error);
}

function formatStatNumber(n: number): string {
  return n.toLocaleString('es-DO');
}
---

<Layout
  title="Sobre Ubíkala | Tu Portal Inmobiliario de Confianza"
  description="Conoce Ubíkala, el portal que conecta a compradores e inquilinos con asesores inmobiliarios, agencias y propietarios."
  keywords="portal inmobiliario, sobre ubíkala, quienes somos, inmobiliarias"
>
  <Breadcrumb items={[
    { label: 'Inicio', href: '/' },
    { label: 'Sobre el Portal' }
  ]} />

  <!-- Hero -->
  <section class="bg-gradient-to-r from-primary-600 to-primary-700 py-16">
    <div class="container-custom text-center">
      <h1 class="text-4xl md:text-5xl font-bold text-white mb-6">
        Tu Portal Inmobiliario de Confianza
      </h1>
      <p class="text-xl text-primary-100 max-w-3xl mx-auto">
        Conectamos a quienes buscan propiedades con los profesionales que las ofrecen.
        Un espacio para asesores, agencias inmobiliarias y propietarios.
      </p>
    </div>
  </section>

  <!-- Qué es PropiedadEnRD -->
  <section class="section">
    <div class="container-custom">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
        <div>
          <span class="text-primary-600 font-semibold text-sm uppercase tracking-wider">
            ¿Qué es Ubíkala?
          </span>
          <h2 class="section-title mt-2 mb-6">
            Un Portal Abierto para el Mercado Inmobiliario
          </h2>
          <div class="prose prose-lg text-gray-600">
            <p>
              <strong>Ubíkala</strong> es una plataforma donde asesores inmobiliarios independientes,
              agencias establecidas y propietarios particulares pueden publicar sus propiedades para
              alcanzar a miles de compradores e inquilinos potenciales.
            </p>
            <p>
              No somos una inmobiliaria. Somos el punto de encuentro entre la oferta y la demanda
              del mercado inmobiliario. Cada propiedad publicada pertenece a un asesor
              o propietario verificado que te atenderá directamente.
            </p>
            <p>
              Ya sea que busques comprar tu primera casa, alquilar un
              apartamento o vender tu propiedad, aquí encontrarás las herramientas
              y los profesionales adecuados.
            </p>
          </div>
        </div>
        <div>
          <img
            src="https://images.unsplash.com/photo-1560518883-ce09059eeffa?w=600&h=500&fit=crop"
            alt="Portal inmobiliario"
            class="rounded-2xl shadow-xl"
          />
        </div>
      </div>
    </div>
  </section>

  <!-- Para Quién -->
  <section class="section bg-gray-50">
    <div class="container-custom">
      <div class="text-center mb-12">
        <h2 class="section-title">¿Para Quién es Este Portal?</h2>
        <p class="section-subtitle mx-auto">
          Diseñado para servir a todos los actores del mercado inmobiliario
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
        <!-- Compradores -->
        <div class="card p-6 text-center">
          <div class="w-16 h-16 bg-primary-100 rounded-full flex items-center justify-center mx-auto mb-4">
            <Icon name="search" class="w-8 h-8 text-primary-600" />
          </div>
          <h3 class="font-semibold text-gray-900 mb-2">Compradores</h3>
          <p class="text-gray-600 text-sm">
            Explora miles de propiedades y conecta directamente con el asesor o propietario.
          </p>
        </div>

        <!-- Inquilinos -->
        <div class="card p-6 text-center">
          <div class="w-16 h-16 bg-secondary-100 rounded-full flex items-center justify-center mx-auto mb-4">
            <Icon name="house" class="w-8 h-8 text-secondary-600" />
          </div>
          <h3 class="font-semibold text-gray-900 mb-2">Inquilinos</h3>
          <p class="text-gray-600 text-sm">
            Encuentra apartamentos y casas en alquiler en todo el país.
          </p>
        </div>

        <!-- Asesores -->
        <div class="card p-6 text-center">
          <div class="w-16 h-16 bg-accent-100 rounded-full flex items-center justify-center mx-auto mb-4">
            <Icon name="user" class="w-8 h-8 text-accent-600" />
          </div>
          <h3 class="font-semibold text-gray-900 mb-2">Asesores</h3>
          <p class="text-gray-600 text-sm">
            Publica tus propiedades y recibe contactos de clientes interesados.
          </p>
        </div>

        <!-- Agencias -->
        <div class="card p-6 text-center">
          <div class="w-16 h-16 bg-purple-100 rounded-full flex items-center justify-center mx-auto mb-4">
            <Icon name="building" class="w-8 h-8 text-purple-600" />
          </div>
          <h3 class="font-semibold text-gray-900 mb-2">Agencias</h3>
          <p class="text-gray-600 text-sm">
            Gestiona tu inventario y aumenta la visibilidad de tu empresa.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Cómo Funciona -->
  <section class="section">
    <div class="container-custom">
      <div class="text-center mb-12">
        <h2 class="section-title">¿Cómo Funciona?</h2>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <div class="text-center">
          <div class="w-20 h-20 bg-primary-600 text-white rounded-full flex items-center justify-center mx-auto mb-6 text-3xl font-bold">
            1
          </div>
          <h3 class="text-xl font-semibold text-gray-900 mb-3">Busca o Publica</h3>
          <p class="text-gray-600">
            Los compradores buscan propiedades. Los asesores y propietarios las publican con fotos y detalles completos.
          </p>
        </div>

        <div class="text-center">
          <div class="w-20 h-20 bg-primary-600 text-white rounded-full flex items-center justify-center mx-auto mb-6 text-3xl font-bold">
            2
          </div>
          <h3 class="text-xl font-semibold text-gray-900 mb-3">Conecta</h3>
          <p class="text-gray-600">
            El interesado contacta directamente al asesor o propietario por teléfono, WhatsApp o formulario.
          </p>
        </div>

        <div class="text-center">
          <div class="w-20 h-20 bg-secondary-600 text-white rounded-full flex items-center justify-center mx-auto mb-6 text-3xl font-bold">
            3
          </div>
          <h3 class="text-xl font-semibold text-gray-900 mb-3">Cierra el Trato</h3>
          <p class="text-gray-600">
            Visita la propiedad, negocia y cierra directamente con quien la ofrece. Nosotros facilitamos el encuentro.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Números -->
  <section class="py-16 bg-primary-600">
    <div class="container-custom">
      <div class="grid grid-cols-3 gap-8 text-center max-w-3xl mx-auto">
        <div>
          <div class="text-4xl md:text-5xl font-bold text-white mb-2">{formatStatNumber(stats.totalProperties)}</div>
          <div class="text-primary-200">Propiedades Activas</div>
        </div>
        <div>
          <div class="text-4xl md:text-5xl font-bold text-white mb-2">{formatStatNumber(stats.totalAgents)}</div>
          <div class="text-primary-200">Asesores Registrados</div>
        </div>
        <div>
          <div class="text-4xl md:text-5xl font-bold text-white mb-2">{formatStatNumber(stats.totalCities)}</div>
          <div class="text-primary-200">Ciudades</div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="section">
    <div class="container-custom">
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <div class="card p-8 text-center border-2 border-primary-100 hover:border-primary-500 transition-colors">
          <h3 class="text-2xl font-bold text-gray-900 mb-4">¿Buscas una Propiedad?</h3>
          <p class="text-gray-600 mb-6">
            Explora nuestro catálogo y encuentra tu próximo hogar o inversión.
          </p>
          <a href="/comprar" class="btn-primary">
            Ver Propiedades
          </a>
        </div>

        <div class="card p-8 text-center border-2 border-secondary-100 hover:border-secondary-500 transition-colors">
          <h3 class="text-2xl font-bold text-gray-900 mb-4">¿Quieres Publicar?</h3>
          <p class="text-gray-600 mb-6">
            Registra tu cuenta y comienza a recibir contactos de clientes.
          </p>
          <a href="/publicar" class="btn-secondary">
            Publicar Propiedad
          </a>
        </div>
      </div>
    </div>
  </section>
</Layout>
