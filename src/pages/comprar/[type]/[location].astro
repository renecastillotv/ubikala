---
import Layout from '../../../layouts/Layout.astro';
import PropertyCard from '../../../components/PropertyCard.astro';
import SearchBox from '../../../components/SearchBox.astro';
import Pagination from '../../../components/Pagination.astro';
import Breadcrumb from '../../../components/Breadcrumb.astro';
import MarketPanorama from '../../../components/MarketPanorama.astro';
import { searchProperties, getSectorsWithCounts } from '../../../lib/meilisearch';
import type { Property } from '../../../data/types';
import { slugToName, slugToCityNames } from '../../../lib/location-utils';
import { toSlug } from '../../../lib/slug-utils';

// SSR para rutas dinámicas
export const prerender = false;

const { type: typeSlug, location: locationSlug } = Astro.params;

// Mapeo de slugs de tipo a valores de BD y nombres legibles
const propertyTypeMap: Record<string, { dbValue: string; singular: string; plural: string; icon: string }> = {
  'apartamento': { dbValue: 'apartamento', singular: 'Apartamento', plural: 'Apartamentos', icon: '🏢' },
  'apartamentos': { dbValue: 'apartamento', singular: 'Apartamento', plural: 'Apartamentos', icon: '🏢' },
  'casa': { dbValue: 'casa', singular: 'Casa', plural: 'Casas', icon: '🏠' },
  'casas': { dbValue: 'casa', singular: 'Casa', plural: 'Casas', icon: '🏠' },
  'villa': { dbValue: 'villa', singular: 'Villa', plural: 'Villas', icon: '🏡' },
  'villas': { dbValue: 'villa', singular: 'Villa', plural: 'Villas', icon: '🏡' },
  'penthouse': { dbValue: 'penthouse', singular: 'Penthouse', plural: 'Penthouses', icon: '🌆' },
  'penthouses': { dbValue: 'penthouse', singular: 'Penthouse', plural: 'Penthouses', icon: '🌆' },
  'terreno': { dbValue: 'terreno', singular: 'Terreno', plural: 'Terrenos', icon: '🌳' },
  'terrenos': { dbValue: 'terreno', singular: 'Terreno', plural: 'Terrenos', icon: '🌳' },
  'solar': { dbValue: 'terreno', singular: 'Solar', plural: 'Solares', icon: '🌳' },
  'solares': { dbValue: 'terreno', singular: 'Solar', plural: 'Solares', icon: '🌳' },
  'local': { dbValue: 'local comercial', singular: 'Local Comercial', plural: 'Locales Comerciales', icon: '🏪' },
  'local-comercial': { dbValue: 'local comercial', singular: 'Local Comercial', plural: 'Locales Comerciales', icon: '🏪' },
  'locales': { dbValue: 'local comercial', singular: 'Local Comercial', plural: 'Locales Comerciales', icon: '🏪' },
  'oficina': { dbValue: 'oficina', singular: 'Oficina', plural: 'Oficinas', icon: '🏛️' },
  'oficinas': { dbValue: 'oficina', singular: 'Oficina', plural: 'Oficinas', icon: '🏛️' },
  'nave': { dbValue: 'nave', singular: 'Nave Industrial', plural: 'Naves Industriales', icon: '🏭' },
  'naves': { dbValue: 'nave', singular: 'Nave Industrial', plural: 'Naves Industriales', icon: '🏭' },
  'finca': { dbValue: 'finca', singular: 'Finca', plural: 'Fincas', icon: '🌾' },
  'fincas': { dbValue: 'finca', singular: 'Finca', plural: 'Fincas', icon: '🌾' },
};

// Validar tipo de propiedad
const typeInfo = propertyTypeMap[typeSlug?.toLowerCase() || ''];
if (!typeInfo) {
  // Redirigir a la página general de comprar si el tipo no es válido
  return Astro.redirect('/comprar');
}

// Configuración de paginación
const ITEMS_PER_PAGE = 24;
const currentPage = Math.max(1, parseInt(Astro.url.searchParams.get('page') || '1'));

// Read advanced filter query params (from SearchBox)
const precioParam = Astro.url.searchParams.get('precio');
const habitacionesParam = Astro.url.searchParams.get('habitaciones');
const banosParam = Astro.url.searchParams.get('banos');
const areaMinParam = Astro.url.searchParams.get('area_min');
const areaMaxParam = Astro.url.searchParams.get('area_max');

let precioMin: number | undefined;
let precioMax: number | undefined;
if (precioParam) {
  if (precioParam.endsWith('+')) {
    precioMin = parseInt(precioParam);
  } else if (precioParam.includes('-')) {
    const [min, max] = precioParam.split('-').map(Number);
    precioMin = min || undefined;
    precioMax = max || undefined;
  }
}

const pais = Astro.locals.country?.name;

// Resolve real city name from slug via MeiliSearch facets
const resolvedName = await slugToName(locationSlug || '', pais);
// Get all equivalent city names for OR filtering (e.g. "Distrito Nacional" + "Santo Domingo D.N.")
const cityNames = await slugToCityNames(locationSlug || '', pais);

let locationData: { name: string; province: string } = {
  name: resolvedName,
  province: pais || ''
};

// Fetch properties con filtros de tipo y ubicación via MeiliSearch
let properties: Property[] = [];
let totalCount = 0;

try {
  const offset = (currentPage - 1) * ITEMS_PER_PAGE;

  const result = await searchProperties({
    operacion: 'venta',
    tipo: typeInfo.dbValue,
    ciudad: cityNames.length > 1 ? cityNames : resolvedName,
    precioMin,
    precioMax,
    habitacionesMin: habitacionesParam ? parseInt(habitacionesParam) : undefined,
    banosMin: banosParam ? parseInt(banosParam) : undefined,
    limit: ITEMS_PER_PAGE,
    offset,
    pais,
  });

  properties = result.properties;
  totalCount = result.total;

  // Update location data from first property if available
  if (properties.length > 0) {
    const firstProp = properties[0];
    if (firstProp.location.city) {
      locationData.name = firstProp.location.city;
    }
    if (firstProp.location.province) {
      locationData.province = firstProp.location.province;
    }
  }
} catch (error) {
  console.error('Error fetching properties:', error);
}

const totalPages = Math.ceil(totalCount / ITEMS_PER_PAGE);

// Redirect si la página está fuera de rango
if (currentPage > totalPages && totalPages > 0) {
  return Astro.redirect(`/comprar/${typeSlug}/${locationSlug}`);
}

// Títulos y descripciones dinámicas para SEO
const pageTitle = `${typeInfo.plural} en Venta en ${locationData.name} | Ubíkala`;
const pageDescription = `Encuentra ${typeInfo.plural.toLowerCase()} en venta en ${locationData.name}, ${locationData.province}. ${totalCount} propiedades disponibles con fotos, precios y contacto directo con asesores.`;

// FAQs dinámicas basadas en tipo y ubicación
const dynamicFAQs = [
  {
    question: `¿Cuánto cuesta un ${typeInfo.singular.toLowerCase()} en ${locationData.name}?`,
    answer: `Los precios de ${typeInfo.plural.toLowerCase()} en ${locationData.name} varían según la ubicación exacta, tamaño y amenidades. Actualmente tenemos ${totalCount} opciones disponibles. Te recomendamos explorar el listado y contactar a los asesores para obtener información actualizada de precios.`
  },
  {
    question: `¿Es buena inversión comprar ${typeInfo.singular.toLowerCase()} en ${locationData.name}?`,
    answer: `${locationData.name} es una zona con demanda estable de ${typeInfo.plural.toLowerCase()}. Para evaluar si es buena inversión, considera factores como la plusvalía histórica del sector, demanda de alquiler, desarrollo de infraestructura y tus objetivos financieros. Un asesor inmobiliario puede darte información específica del mercado local.`
  },
  {
    question: `¿Qué documentos necesito para comprar un ${typeInfo.singular.toLowerCase()}?`,
    answer: `Para comprar un ${typeInfo.singular.toLowerCase()} en ${pais} necesitas: cédula o pasaporte, constancia de ingresos o estados de cuenta, y el inicial (generalmente 10-30% del valor). El vendedor debe proveer título de propiedad certificado, plano catastral y certificación de no gravamen.`
  },
  {
    question: `¿Puedo financiar un ${typeInfo.singular.toLowerCase()} en ${locationData.name}?`,
    answer: `Sí, la mayoría de los ${typeInfo.plural.toLowerCase()} pueden financiarse a través de bancos locales. Las tasas actuales rondan el 9-12% anual para plazos de 15-25 años. Algunos proyectos nuevos ofrecen financiamiento directo con el desarrollador con condiciones más flexibles.`
  }
];

// Schema para SEO
const listingSchema = {
  '@context': 'https://schema.org',
  '@type': 'ItemList',
  name: `${typeInfo.plural} en Venta en ${locationData.name}`,
  description: pageDescription,
  numberOfItems: totalCount,
  itemListElement: properties.slice(0, 10).map((property, index) => ({
    '@type': 'ListItem',
    position: index + 1,
    item: {
      '@type': 'RealEstateListing',
      name: property.title,
      description: property.description.substring(0, 160),
      url: `https://ubikala.com/propiedad/${property.slug}`,
      image: property.images[0]?.url,
      offers: {
        '@type': 'Offer',
        price: property.price,
        priceCurrency: property.currency,
      },
    },
  })),
};

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: dynamicFAQs.map(faq => ({
    '@type': 'Question',
    name: faq.question,
    acceptedAnswer: {
      '@type': 'Answer',
      text: faq.answer
    }
  }))
};

// When empty: fetch alternatives so user can navigate to where there IS content
let altCitiesForType: Array<{ name: string; slug: string; count: number }> = [];
let altTypesInCity: Array<{ name: string; slug: string; count: number; icon: string }> = [];

if (totalCount === 0) {
  try {
    // Cities that DO have this type for sale
    const citiesResult = await searchProperties({
      tipo: typeInfo.dbValue,
      operacion: 'venta',
      pais,
      limit: 0,
      facets: ['ciudad'],
    });
    const cityDist = citiesResult.facetDistribution?.ciudad || {};
    altCitiesForType = Object.entries(cityDist)
      .map(([name, count]) => ({ name, slug: toSlug(name), count: count as number }))
      .sort((a, b) => b.count - a.count)
      .slice(0, 6);

    // Types available for sale in THIS city
    const typesResult = await searchProperties({
      operacion: 'venta',
      ciudad: cityNames.length > 1 ? cityNames : resolvedName,
      pais,
      limit: 0,
      facets: ['tipo'],
    });
    const typeDist = typesResult.facetDistribution?.tipo || {};
    const tipoDisplay: Record<string, { plural: string; slug: string; icon: string }> = {
      'apartamento': { plural: 'Apartamentos', slug: 'apartamento', icon: '🏢' },
      'casa': { plural: 'Casas', slug: 'casa', icon: '🏠' },
      'villa': { plural: 'Villas', slug: 'villa', icon: '🏡' },
      'penthouse': { plural: 'Penthouses', slug: 'penthouse', icon: '🌆' },
      'terreno': { plural: 'Terrenos', slug: 'terreno', icon: '🌳' },
      'local comercial': { plural: 'Locales Comerciales', slug: 'local-comercial', icon: '🏪' },
      'oficina': { plural: 'Oficinas', slug: 'oficina', icon: '🏛️' },
      'nave': { plural: 'Naves Industriales', slug: 'nave', icon: '🏭' },
      'finca': { plural: 'Fincas', slug: 'finca', icon: '🌾' },
      'estudio': { plural: 'Estudios', slug: 'estudio', icon: '🛏️' },
    };
    altTypesInCity = Object.entries(typeDist)
      .filter(([dbType]) => dbType !== typeInfo.dbValue && tipoDisplay[dbType])
      .map(([dbType, count]) => ({
        name: tipoDisplay[dbType].plural,
        slug: tipoDisplay[dbType].slug,
        count: count as number,
        icon: tipoDisplay[dbType].icon,
      }))
      .sort((a, b) => b.count - a.count)
      .slice(0, 6);
  } catch { /* ignore */ }
}

// Tipos relacionados para sugerencias (when there ARE results)
const relatedTypes = totalCount > 0
  ? Object.entries(propertyTypeMap)
      .filter(([slug, info]) => info.dbValue !== typeInfo.dbValue && !slug.endsWith('s'))
      .slice(0, 4)
      .map(([slug, info]) => ({ slug, ...info }))
  : [];

// Sectors in this city for deep internal links (only when there are results)
let popularSectors: Array<{ name: string; slug: string; count: number }> = [];
if (totalCount > 0) {
  try {
    popularSectors = await getSectorsWithCounts(cityNames.length > 1 ? cityNames : resolvedName, pais);
    popularSectors = popularSectors.slice(0, 12);
  } catch { /* ignore */ }
}
---

<Layout
  title={pageTitle}
  description={pageDescription}
  keywords={`${typeInfo.plural.toLowerCase()} ${locationData.name.toLowerCase()}, comprar ${typeInfo.singular.toLowerCase()} ${locationData.name.toLowerCase()}, ${typeInfo.plural.toLowerCase()} venta ${locationData.name.toLowerCase()}`}
>
  <script type="application/ld+json" set:html={JSON.stringify(listingSchema)} slot="head" />
  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} slot="head" />

  <Breadcrumb items={[
    { label: 'Inicio', href: '/' },
    { label: 'Comprar', href: '/comprar' },
    { label: typeInfo.plural, href: `/comprar/${typeSlug}` },
    { label: locationData.name }
  ]} />

  <!-- Hero -->
  <section class="bg-gradient-to-r from-primary-600 to-primary-700 py-12">
    <div class="container-custom">
      <h1 class="text-3xl md:text-4xl font-bold text-white mb-4">
        {typeInfo.plural} en Venta en {locationData.name}
      </h1>
      <p class="text-primary-100 text-lg max-w-3xl">
        {totalCount > 0
          ? `Explora ${totalCount} ${typeInfo.plural.toLowerCase()} disponibles para compra en ${locationData.name}, ${locationData.province}.`
          : `Actualmente no hay ${typeInfo.plural.toLowerCase()} disponibles en ${locationData.name}. Explora otras zonas o tipos de propiedad.`
        }
      </p>
    </div>
  </section>

  <!-- Search (collapsible) -->
  <section class="py-4 bg-gray-50 border-b">
    <div class="container-custom">
      <details>
        <summary class="cursor-pointer text-sm font-medium text-primary-600 hover:text-primary-700 flex items-center gap-2">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 4a1 1 0 011-1h16a1 1 0 011 1v2.586a1 1 0 01-.293.707l-6.414 6.414a1 1 0 00-.293.707V17l-4 4v-6.586a1 1 0 00-.293-.707L3.293 7.293A1 1 0 013 6.586V4z" />
          </svg>
          Modificar Busqueda
        </summary>
        <div class="mt-4">
          <SearchBox />
        </div>
      </details>
    </div>
  </section>

  <!-- Results or Empty State -->
  <section class="section">
    <div class="container-custom">
      {properties.length > 0 ? (
        <>
          <div class="flex items-center justify-between mb-8">
            <p class="text-gray-600">
              <span class="font-semibold text-gray-900">{totalCount}</span> {typeInfo.plural.toLowerCase()} en venta
            </p>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            {properties.map((property) => (
              <PropertyCard property={property} />
            ))}
          </div>

          {totalPages > 1 && (
            <div class="mt-12">
              <Pagination currentPage={currentPage} totalPages={totalPages} baseUrl={`/comprar/${typeSlug}/${locationSlug}`} />
            </div>
          )}
        </>
      ) : (
        <div class="py-12">
          <div class="text-center mb-12">
            <div class="w-20 h-20 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-6">
              <svg class="w-10 h-10 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
              </svg>
            </div>
            <h2 class="text-2xl font-bold text-gray-900 mb-2">
              No hay {typeInfo.plural.toLowerCase()} en venta en {locationData.name}
            </h2>
            <p class="text-gray-600 max-w-md mx-auto">
              Pero puedes explorar estas alternativas:
            </p>
          </div>

          {/* Same type in other cities */}
          {altCitiesForType.length > 0 && (
            <div class="mb-10">
              <h3 class="text-lg font-semibold text-gray-900 mb-4">
                {typeInfo.plural} en venta en otras ciudades
              </h3>
              <div class="grid grid-cols-2 md:grid-cols-3 gap-3">
                {altCitiesForType.map((city) => (
                  <a
                    href={`/comprar/${typeSlug}/${city.slug}`}
                    class="bg-white rounded-xl p-4 shadow-sm border border-gray-100 hover:shadow-md hover:border-primary-200 transition-all group"
                  >
                    <span class="font-semibold text-gray-900 group-hover:text-primary-600 block">
                      {city.name}
                    </span>
                    <span class="text-sm text-gray-500">{city.count} {typeInfo.plural.toLowerCase()}</span>
                  </a>
                ))}
              </div>
            </div>
          )}

          {/* Other types in this city */}
          {altTypesInCity.length > 0 && (
            <div>
              <h3 class="text-lg font-semibold text-gray-900 mb-4">
                Otros tipos en venta en {locationData.name}
              </h3>
              <div class="grid grid-cols-2 md:grid-cols-3 gap-3">
                {altTypesInCity.map((tipo) => (
                  <a
                    href={`/comprar/${tipo.slug}/${locationSlug}`}
                    class="bg-white rounded-xl p-4 shadow-sm border border-gray-100 hover:shadow-md hover:border-primary-200 transition-all group"
                  >
                    <span class="text-xl mr-2">{tipo.icon}</span>
                    <span class="font-semibold text-gray-900 group-hover:text-primary-600">
                      {tipo.name}
                    </span>
                    <span class="text-sm text-gray-500 block">{tipo.count} en venta</span>
                  </a>
                ))}
              </div>
            </div>
          )}

          {/* Fallback if no alternatives at all */}
          {altCitiesForType.length === 0 && altTypesInCity.length === 0 && (
            <div class="text-center">
              <a href="/comprar" class="btn-primary">Ver todas las propiedades en venta</a>
            </div>
          )}
        </div>
      )}
    </div>
  </section>

  <!-- Market Panorama SEO Section (data-driven from motor-seo API) -->
  <MarketPanorama
    propertyType={typeSlug}
    locationSlug={locationSlug}
    operation="comprar"
    countryCode={Astro.locals.country?.code}
    language="es"
  />

  <!-- FAQs -->
  <section class="py-16">
    <div class="container-custom">
      <h2 class="text-2xl md:text-3xl font-bold text-gray-900 text-center mb-4">
        Preguntas Frecuentes
      </h2>
      <p class="text-gray-600 text-center max-w-2xl mx-auto mb-12">
        Resolvemos tus dudas sobre comprar {typeInfo.plural.toLowerCase()} en {locationData.name}
      </p>

      <div class="max-w-3xl mx-auto space-y-4">
        {dynamicFAQs.map((faq) => (
          <details class="bg-white rounded-xl shadow-sm group">
            <summary class="flex items-center justify-between p-6 cursor-pointer">
              <span class="font-semibold text-gray-900 pr-4">{faq.question}</span>
              <svg class="w-5 h-5 text-gray-500 group-open:rotate-180 transition-transform flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
              </svg>
            </summary>
            <div class="px-6 pb-6 text-gray-600">
              {faq.answer}
            </div>
          </details>
        ))}
      </div>
    </div>
  </section>

  <!-- Sectors in this city (deep SEO links) -->
  {popularSectors.length > 0 && (
    <section class="py-16 bg-gray-50">
      <div class="container-custom">
        <h2 class="text-2xl font-bold text-gray-900 mb-6">
          {typeInfo.plural} en Venta por Sector en {locationData.name}
        </h2>
        <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
          {popularSectors.map((s) => (
            <a
              href={`/comprar/${typeSlug}/${locationSlug}/${s.slug}`}
              class="bg-white rounded-xl p-4 shadow-sm hover:shadow-md transition-shadow group"
            >
              <span class="font-semibold text-gray-900 group-hover:text-primary-600 block">
                {s.name}
              </span>
              <span class="text-sm text-gray-500">{s.count} {typeInfo.plural.toLowerCase()}</span>
            </a>
          ))}
        </div>
      </div>
    </section>
  )}

  <!-- Otros tipos de propiedad -->
  <section class="py-16 {popularSectors.length === 0 ? 'bg-gray-50' : ''}">
    <div class="container-custom">
      <h2 class="text-2xl font-bold text-gray-900 mb-6">
        Otros Tipos de Propiedad en {locationData.name}
      </h2>
      <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
        {relatedTypes.map((type) => (
          <a
            href={`/comprar/${type.slug}/${locationSlug}`}
            class="bg-white rounded-xl p-6 shadow-sm hover:shadow-md transition-shadow text-center group"
          >
            <span class="text-3xl block mb-2">{type.icon}</span>
            <span class="font-semibold text-gray-900 group-hover:text-primary-600">
              {type.plural}
            </span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="py-16 bg-gradient-to-r from-primary-600 to-primary-700">
    <div class="container-custom">
      <div class="max-w-3xl mx-auto text-center">
        <h2 class="text-3xl font-bold text-white mb-4">
          ¿Necesitas ayuda para encontrar tu {typeInfo.singular.toLowerCase()}?
        </h2>
        <p class="text-primary-100 text-lg mb-8">
          Nuestros asesores conocen {locationData.name} y pueden ayudarte a encontrar la propiedad perfecta según tu presupuesto y necesidades.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a href="/asesores" class="bg-white text-primary-600 hover:bg-primary-50 font-semibold px-8 py-3 rounded-lg transition-colors">
            Contactar un Asesor
          </a>
          <a href="/comprar" class="border-2 border-white text-white hover:bg-white/10 font-semibold px-8 py-3 rounded-lg transition-colors">
            Ver Más Propiedades
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Links Relacionados -->
  <section class="py-12">
    <div class="container-custom">
      <h2 class="text-xl font-bold text-gray-900 mb-6">Enlaces Relacionados</h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
        <a href={`/propiedades/${locationSlug}`} class="flex items-center gap-3 p-4 bg-gray-50 rounded-lg hover:bg-gray-100 transition-colors group">
          <div class="w-10 h-10 bg-primary-100 rounded-lg flex items-center justify-center">
            <svg class="w-5 h-5 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
            </svg>
          </div>
          <div>
            <span class="font-semibold text-gray-900 group-hover:text-primary-600">Todo en {locationData.name}</span>
            <p class="text-sm text-gray-500">Todas las propiedades</p>
          </div>
        </a>

        <a href={`/alquilar?ubicacion=${locationSlug}`} class="flex items-center gap-3 p-4 bg-gray-50 rounded-lg hover:bg-gray-100 transition-colors group">
          <div class="w-10 h-10 bg-secondary-100 rounded-lg flex items-center justify-center">
            <svg class="w-5 h-5 text-secondary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
            </svg>
          </div>
          <div>
            <span class="font-semibold text-gray-900 group-hover:text-primary-600">Alquilar en {locationData.name}</span>
            <p class="text-sm text-gray-500">Propiedades en alquiler</p>
          </div>
        </a>

        <a href="/guias/como-comprar" class="flex items-center gap-3 p-4 bg-gray-50 rounded-lg hover:bg-gray-100 transition-colors group">
          <div class="w-10 h-10 bg-green-100 rounded-lg flex items-center justify-center">
            <svg class="w-5 h-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
            </svg>
          </div>
          <div>
            <span class="font-semibold text-gray-900 group-hover:text-primary-600">Guía de Compra</span>
            <p class="text-sm text-gray-500">Paso a paso</p>
          </div>
        </a>
      </div>
    </div>
  </section>
</Layout>
