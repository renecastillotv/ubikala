---
import Layout from '../../../../layouts/Layout.astro';
import PropertyCard from '../../../../components/PropertyCard.astro';
import SearchBox from '../../../../components/SearchBox.astro';
import Pagination from '../../../../components/Pagination.astro';
import Breadcrumb from '../../../../components/Breadcrumb.astro';
import { searchProperties, getSectorsWithCounts } from '../../../../lib/meilisearch';
import type { Property } from '../../../../data/types';
import { slugToName, slugToCityNames, sectorSlugToName } from '../../../../lib/location-utils';
import { toSlug } from '../../../../lib/slug-utils';

export const prerender = false;

const { type: typeSlug, location: locationSlug, sector: sectorSlug } = Astro.params;

const propertyTypeMap: Record<string, { dbValue: string; singular: string; plural: string; icon: string }> = {
  'apartamento': { dbValue: 'apartamento', singular: 'Apartamento', plural: 'Apartamentos', icon: '🏢' },
  'apartamentos': { dbValue: 'apartamento', singular: 'Apartamento', plural: 'Apartamentos', icon: '🏢' },
  'casa': { dbValue: 'casa', singular: 'Casa', plural: 'Casas', icon: '🏠' },
  'casas': { dbValue: 'casa', singular: 'Casa', plural: 'Casas', icon: '🏠' },
  'villa': { dbValue: 'villa', singular: 'Villa', plural: 'Villas', icon: '🏡' },
  'villas': { dbValue: 'villa', singular: 'Villa', plural: 'Villas', icon: '🏡' },
  'penthouse': { dbValue: 'penthouse', singular: 'Penthouse', plural: 'Penthouses', icon: '🌆' },
  'penthouses': { dbValue: 'penthouse', singular: 'Penthouse', plural: 'Penthouses', icon: '🌆' },
  'local': { dbValue: 'local comercial', singular: 'Local Comercial', plural: 'Locales Comerciales', icon: '🏪' },
  'local-comercial': { dbValue: 'local comercial', singular: 'Local Comercial', plural: 'Locales Comerciales', icon: '🏪' },
  'locales': { dbValue: 'local comercial', singular: 'Local Comercial', plural: 'Locales Comerciales', icon: '🏪' },
  'oficina': { dbValue: 'oficina', singular: 'Oficina', plural: 'Oficinas', icon: '🏛️' },
  'oficinas': { dbValue: 'oficina', singular: 'Oficina', plural: 'Oficinas', icon: '🏛️' },
  'nave': { dbValue: 'nave', singular: 'Nave Industrial', plural: 'Naves Industriales', icon: '🏭' },
  'naves': { dbValue: 'nave', singular: 'Nave Industrial', plural: 'Naves Industriales', icon: '🏭' },
  'estudio': { dbValue: 'estudio', singular: 'Estudio', plural: 'Estudios', icon: '🛏️' },
  'estudios': { dbValue: 'estudio', singular: 'Estudio', plural: 'Estudios', icon: '🛏️' },
};

const typeInfo = propertyTypeMap[typeSlug?.toLowerCase() || ''];
if (!typeInfo) {
  return Astro.redirect('/alquilar');
}

const ITEMS_PER_PAGE = 24;
const currentPage = Math.max(1, parseInt(Astro.url.searchParams.get('page') || '1'));

const pais = Astro.locals.country?.name;

// Resolve city and sector from slugs
const resolvedCityName = await slugToName(locationSlug || '', pais);
const cityNames = await slugToCityNames(locationSlug || '', pais);
const resolvedSectorName = await sectorSlugToName(sectorSlug || '', cityNames, pais);

let locationData = {
  city: resolvedCityName,
  sector: resolvedSectorName,
  province: pais || '',
};

// Read advanced filter query params
const precioParam = Astro.url.searchParams.get('precio');
const habitacionesParam = Astro.url.searchParams.get('habitaciones');
const banosParam = Astro.url.searchParams.get('banos');

let precioMin: number | undefined;
let precioMax: number | undefined;
if (precioParam) {
  if (precioParam.endsWith('+')) {
    precioMin = parseInt(precioParam);
  } else if (precioParam.includes('-')) {
    const [min, max] = precioParam.split('-').map(Number);
    precioMin = min || undefined;
    precioMax = max || undefined;
  }
}

let properties: Property[] = [];
let totalCount = 0;

try {
  const offset = (currentPage - 1) * ITEMS_PER_PAGE;

  const result = await searchProperties({
    operacion: 'renta',
    tipo: typeInfo.dbValue,
    ciudad: cityNames.length > 1 ? cityNames : resolvedCityName,
    sector: resolvedSectorName,
    precioMin,
    precioMax,
    habitacionesMin: habitacionesParam ? parseInt(habitacionesParam) : undefined,
    banosMin: banosParam ? parseInt(banosParam) : undefined,
    limit: ITEMS_PER_PAGE,
    offset,
    pais,
  });

  properties = result.properties;
  totalCount = result.total;

  if (properties.length > 0) {
    const firstProp = properties[0];
    if (firstProp.location.city) locationData.city = firstProp.location.city;
    if (firstProp.location.province) locationData.province = firstProp.location.province;
    if (firstProp.location.sector) locationData.sector = firstProp.location.sector;
  }
} catch (error) {
  console.error('Error fetching properties:', error);
}

const totalPages = Math.ceil(totalCount / ITEMS_PER_PAGE);

if (currentPage > totalPages && totalPages > 0) {
  return Astro.redirect(`/alquilar/${typeSlug}/${locationSlug}/${sectorSlug}`);
}

const pageTitle = `${typeInfo.plural} en Alquiler en ${locationData.sector}, ${locationData.city} | Ubikala`;
const pageDescription = `Encuentra ${typeInfo.plural.toLowerCase()} en alquiler en ${locationData.sector}, ${locationData.city}. ${totalCount} propiedades disponibles con fotos, precios y contacto directo.`;

const dynamicFAQs = [
  {
    question: `¿Cuanto cuesta alquilar un ${typeInfo.singular.toLowerCase()} en ${locationData.sector}?`,
    answer: `Los precios de alquiler de ${typeInfo.plural.toLowerCase()} en ${locationData.sector}, ${locationData.city} varian segun el tamano, piso y amenidades. Actualmente tenemos ${totalCount} opciones disponibles. Contacta a los asesores para precios actualizados.`
  },
  {
    question: `¿Que documentos necesito para alquilar en ${locationData.sector}?`,
    answer: `Generalmente necesitas: cedula o pasaporte, carta de trabajo o comprobantes de ingresos, referencias personales y deposito de seguridad (usualmente 1-2 meses). Cada propietario puede tener requisitos adicionales.`
  },
  {
    question: `¿Es ${locationData.sector} una buena zona para vivir?`,
    answer: `${locationData.sector} es un sector de ${locationData.city} con oferta de ${typeInfo.plural.toLowerCase()} en alquiler. Te recomendamos visitar la zona y consultar con un asesor local para conocer detalles sobre servicios, transporte y estilo de vida.`
  },
];

const listingSchema = {
  '@context': 'https://schema.org',
  '@type': 'ItemList',
  name: `${typeInfo.plural} en Alquiler en ${locationData.sector}, ${locationData.city}`,
  description: pageDescription,
  numberOfItems: totalCount,
  itemListElement: properties.slice(0, 10).map((property, index) => ({
    '@type': 'ListItem',
    position: index + 1,
    item: {
      '@type': 'RealEstateListing',
      name: property.title,
      description: property.description?.substring(0, 160),
      url: `https://ubikala.com/propiedad/${property.slug}`,
      image: property.images[0]?.url,
      offers: {
        '@type': 'Offer',
        price: property.rentalPrice || property.price,
        priceCurrency: property.currency,
      },
    },
  })),
};

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: dynamicFAQs.map(faq => ({
    '@type': 'Question',
    name: faq.question,
    acceptedAnswer: { '@type': 'Answer', text: faq.answer },
  })),
};

let relatedSectors: Array<{ name: string; slug: string; count: number }> = [];
try {
  const allSectors = await getSectorsWithCounts(cityNames.length > 1 ? cityNames : resolvedCityName, pais);
  relatedSectors = allSectors
    .filter(s => toSlug(s.name) !== sectorSlug)
    .slice(0, 8);
} catch { /* ignore */ }

const relatedTypes = Object.entries(propertyTypeMap)
  .filter(([slug, info]) => info.dbValue !== typeInfo.dbValue && !slug.endsWith('s') && slug !== 'local')
  .slice(0, 4)
  .map(([slug, info]) => ({ slug, ...info }));
---

<Layout
  title={pageTitle}
  description={pageDescription}
  keywords={`alquilar ${typeInfo.singular.toLowerCase()} ${locationData.sector.toLowerCase()}, ${typeInfo.plural.toLowerCase()} alquiler ${locationData.sector.toLowerCase()} ${locationData.city.toLowerCase()}`}
>
  <script type="application/ld+json" set:html={JSON.stringify(listingSchema)} slot="head" />
  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} slot="head" />

  <Breadcrumb items={[
    { label: 'Inicio', href: '/' },
    { label: 'Alquilar', href: '/alquilar' },
    { label: typeInfo.plural, href: `/alquilar/${typeSlug}` },
    { label: locationData.city, href: `/alquilar/${typeSlug}/${locationSlug}` },
    { label: locationData.sector },
  ]} />

  <section class="bg-gradient-to-r from-secondary-600 to-secondary-700 py-12">
    <div class="container-custom">
      <h1 class="text-3xl md:text-4xl font-bold text-white mb-4">
        {typeInfo.plural} en Alquiler en {locationData.sector}
      </h1>
      <p class="text-secondary-100 text-lg max-w-3xl">
        {totalCount > 0
          ? `Explora ${totalCount} ${typeInfo.plural.toLowerCase()} disponibles para alquiler en ${locationData.sector}, ${locationData.city}.`
          : `Actualmente no hay ${typeInfo.plural.toLowerCase()} en alquiler en ${locationData.sector}. Explora otros sectores.`
        }
      </p>
    </div>
  </section>

  <section class="py-4 bg-gray-50 border-b">
    <div class="container-custom">
      <details>
        <summary class="cursor-pointer text-sm font-medium text-secondary-600 hover:text-secondary-700 flex items-center gap-2">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 4a1 1 0 011-1h16a1 1 0 011 1v2.586a1 1 0 01-.293.707l-6.414 6.414a1 1 0 00-.293.707V17l-4 4v-6.586a1 1 0 00-.293-.707L3.293 7.293A1 1 0 013 6.586V4z" />
          </svg>
          Modificar Busqueda
        </summary>
        <div class="mt-4">
          <SearchBox />
        </div>
      </details>
    </div>
  </section>

  <section class="section">
    <div class="container-custom">
      {properties.length > 0 ? (
        <>
          <div class="flex items-center justify-between mb-8">
            <p class="text-gray-600">
              <span class="font-semibold text-gray-900">{totalCount}</span> {typeInfo.plural.toLowerCase()} en alquiler en {locationData.sector}
            </p>
          </div>

          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            {properties.map((property) => (
              <PropertyCard property={property} />
            ))}
          </div>

          {totalPages > 1 && (
            <div class="mt-12">
              <Pagination currentPage={currentPage} totalPages={totalPages} baseUrl={`/alquilar/${typeSlug}/${locationSlug}/${sectorSlug}`} />
            </div>
          )}
        </>
      ) : (
        <div class="text-center py-16">
          <div class="w-24 h-24 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-6">
            <svg class="w-12 h-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
            </svg>
          </div>
          <h2 class="text-2xl font-bold text-gray-900 mb-4">
            No encontramos {typeInfo.plural.toLowerCase()} en alquiler en {locationData.sector}
          </h2>
          <p class="text-gray-600 mb-8 max-w-md mx-auto">
            Prueba con otro sector o explora todas las propiedades en {locationData.city}.
          </p>
          <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <a href={`/alquilar/${typeSlug}/${locationSlug}`} class="btn-primary">
              Ver en {locationData.city}
            </a>
            <a href="/alquilar" class="btn-outline">
              Ver Todas las Propiedades
            </a>
          </div>
        </div>
      )}
    </div>
  </section>

  <!-- FAQs -->
  <section class="py-16">
    <div class="container-custom">
      <h2 class="text-2xl md:text-3xl font-bold text-gray-900 text-center mb-4">
        Preguntas Frecuentes
      </h2>
      <p class="text-gray-600 text-center max-w-2xl mx-auto mb-12">
        Resolvemos tus dudas sobre alquilar {typeInfo.plural.toLowerCase()} en {locationData.sector}
      </p>
      <div class="max-w-3xl mx-auto space-y-4">
        {dynamicFAQs.map((faq) => (
          <details class="bg-white rounded-xl shadow-sm group">
            <summary class="flex items-center justify-between p-6 cursor-pointer">
              <span class="font-semibold text-gray-900 pr-4">{faq.question}</span>
              <svg class="w-5 h-5 text-gray-500 group-open:rotate-180 transition-transform flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
              </svg>
            </summary>
            <div class="px-6 pb-6 text-gray-600">{faq.answer}</div>
          </details>
        ))}
      </div>
    </div>
  </section>

  {relatedSectors.length > 0 && (
    <section class="py-16 bg-gray-50">
      <div class="container-custom">
        <h2 class="text-2xl font-bold text-gray-900 mb-6">
          Otros Sectores en {locationData.city}
        </h2>
        <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
          {relatedSectors.map((s) => (
            <a
              href={`/alquilar/${typeSlug}/${locationSlug}/${s.slug}`}
              class="bg-white rounded-xl p-4 shadow-sm hover:shadow-md transition-shadow group"
            >
              <span class="font-semibold text-gray-900 group-hover:text-secondary-600 block">{s.name}</span>
              <span class="text-sm text-gray-500">{s.count} {typeInfo.plural.toLowerCase()}</span>
            </a>
          ))}
        </div>
      </div>
    </section>
  )}

  <section class="py-16">
    <div class="container-custom">
      <h2 class="text-2xl font-bold text-gray-900 mb-6">
        Otros Tipos de Propiedad en {locationData.sector}
      </h2>
      <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
        {relatedTypes.map((type) => (
          <a
            href={`/alquilar/${type.slug}/${locationSlug}/${sectorSlug}`}
            class="bg-white rounded-xl p-6 shadow-sm hover:shadow-md transition-shadow text-center group"
          >
            <span class="text-3xl block mb-2">{type.icon}</span>
            <span class="font-semibold text-gray-900 group-hover:text-secondary-600">{type.plural}</span>
          </a>
        ))}
      </div>
    </div>
  </section>

  <section class="py-16 bg-gradient-to-r from-secondary-600 to-secondary-700">
    <div class="container-custom">
      <div class="max-w-3xl mx-auto text-center">
        <h2 class="text-3xl font-bold text-white mb-4">
          ¿Buscas alquilar en {locationData.sector}?
        </h2>
        <p class="text-secondary-100 text-lg mb-8">
          Nuestros asesores conocen {locationData.sector} y pueden ayudarte a encontrar el alquiler ideal.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a href="/asesores" class="bg-white text-secondary-600 hover:bg-secondary-50 font-semibold px-8 py-3 rounded-lg transition-colors">
            Contactar un Asesor
          </a>
          <a href={`/alquilar/${typeSlug}/${locationSlug}`} class="border-2 border-white text-white hover:bg-white/10 font-semibold px-8 py-3 rounded-lg transition-colors">
            Ver Todo en {locationData.city}
          </a>
        </div>
      </div>
    </div>
  </section>
</Layout>
